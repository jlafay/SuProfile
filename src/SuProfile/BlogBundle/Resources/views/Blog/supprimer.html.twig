{% extends "SuProfileBlogBundle::layout.html.twig" %}
 
{% block title %}
	Supprimer la publication - {{ parent() }}
{% endblock %}
 
{% block SuProfileblog_body %}
	<div class="border">
		<h3>Supprimer la publication</h3></br>
 
		<p>
			Etes-vous certain de vouloir supprimer la publication "{{ article.titre }}" ?
		</p>
 
		<form action="{{ path('SuProfileblog_supprimer', {'id': article.id}) }}" method="post">
		<a href="{{ path('SuProfileblog_voir', {'slug': article.slug}) }}" class="btn"><i class="icon-chevron-left"></i>Retour à la publication</a>
		<input type="submit" value="Supprimer" class="btn btn-danger" />
			{{ form_rest(form) }}
		</form>
	</div>
{% endblock %}